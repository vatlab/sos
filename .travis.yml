language: python
python:
    - "3.6"
before_install:
    - sudo apt-get update
    - wget https://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh
    - bash miniconda.sh -b -p $HOME/miniconda
    - export PATH="$HOME/miniconda/bin:$PATH"
    - hash -r
    - conda config --set always_yes yes --set changeps1 no
    - conda update -q conda
    # Useful for debugging any issues with conda
    - conda info -a
    # add docker
    - pip install docker nose
    - pip install spyder notebook nbconvert nbformat pyyaml psutil tqdm
    - pip install fasteners pygments ipython ptpython networkx pydotplus
    # install r kernel
    - conda install -c r r-essentials
    - conda install -c conda-forge feather-format
    # install bash kernel
    - pip install bash_kernel
    - python -m bash_kernel.install
    # install python 2.x kernel
    - conda create -n ipykernel_py2 python=2 ipykernel
    - source activate ipykernel_py2
    - python -m ipykernel install --user
    - source deactivate ipykernel_py2
    # get docker images used by the tests
    - docker pull ubuntu
    - docker pull python
    - docker pull python
    - docker pull ruby
    - docker pull node
    - docker pull r-base
    - docker pull compbio/ngseasy-fastqc:1.0-r001
    - docker pull blang/busybox-bash
sudo: required
services:
    - docker
install: "python setup.py install"
before_script: cd test
script: ./run_tests
notifications:
    email:
        recipients:
            - ben.bob@gmail.com
        on_success: never
        on_failure: always
